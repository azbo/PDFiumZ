# 03-PageManipulation - 页面操作

本目录演示 PDF 文档的页面操作功能。

## 📚 示例列表

### RangeOperations
使用现代 Range 语法进行页面操作（.NET 8+）：
- 获取前 N 页：`..10`
- 获取指定范围：`5..15`
- 获取最后 N 页：`^5..`
- 删除页面范围
- 移动页面
- 使用 Range 生成图像

### MergeSplit
PDF 文档的合并与拆分操作：
- 合并多个 PDF 文件
- 提取指定页面
- 拆分每个页面为单独文件
- 使用 Range 语法拆分（.NET 8+）
- 旋转页面
- 删除页面

## 🚀 运行示例

```bash
# 编译
dotnet build

# 运行
dotnet run
```

**注意**：需要 .NET 8.0 或更高版本。

## 💡 Range 语法

### 基本语法

| 语法 | 说明 | 等价的传统方式 |
|------|------|----------------|
| `..10` | 前 10 页 | `GetPages(0, 10)` |
| `5..15` | 页面 5-14 | `GetPages(5, 10)` |
| `^5..` | 最后 5 页 | `GetPages(PageCount-5, 5)` |
| `..^10` | 除最后 10 页外的所有页 | - |
| `5..` | 从第 5 页到末尾 | `GetPages(5, PageCount-5)` |
| `..10` | 从开始到第 10 页 | `GetPages(0, 10)` |

### 页面操作示例

```csharp
using var document = PdfDocument.Open("sample.pdf");

// 获取页面
var pages = document.GetPages(..10);  // 前 10 页

// 删除页面
document.DeletePages(..3);  // 删除前 3 页

// 移动页面
document.MovePages(0, ^5..);  // 将最后 5 页移到开头

// 拆分文档
using var extracted = document.Split(5..15);  // 提取页面 5-14
```

## 🔧 兼容性

### .NET 8+
完全支持 Range 语法：
```csharp
var pages = document.GetPages(..10);
```

### .NET 7 及更早版本
使用传统语法：
```csharp
var pages = document.GetPages(0, 10);
```

## 📖 相关 API

- `PdfDocument.GetPages()` - 获取页面
- `PdfDocument.DeletePages()` - 删除页面
- `PdfDocument.MovePages()` - 移动页面
- `PdfDocument.Split()` - 拆分文档

---

**返回示例目录**：[README](../README.md)
